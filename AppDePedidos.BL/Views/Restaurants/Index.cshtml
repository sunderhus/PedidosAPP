@model IEnumerable<AppDePedidos.Data.Models.Restaurante>

@{
    ViewBag.Title = "Index";

}

<h2>Index</h2>
<div class="btn btn-default">
    @Html.ActionLink("Adicionar", "Create")
</div>
<br/><br/>

@foreach (var item in Model)
{
    @Html.Partial("_RestauranteSumario",item)
}

@section footer{
   <p> CRUD - API - SGBD SQLServer</p>
}